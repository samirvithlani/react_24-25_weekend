import React from 'react'

export const Collage = (props) => {

  return (
    <div>
        <h1>Collage</h1>
        <button onClick={()=>{props.check()}}>check uni from collage..</button>
    </div>
  )
}
